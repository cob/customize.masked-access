# customize.masked-access

## Setup:

### Add the definition to RecordM:

```text
Definition: /others/customize.masked-access/definitions/definition_masked_access_audit.json
```

### Create permission:

```text
actions:execute:_masked-access-get-values
product: integrationm
```